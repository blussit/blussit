fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
motor==3.6.0
pymongo==4.9.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
python-dotenv==1.0.1
email-validator==2.2.0
bcrypt==4.0.1

# Test suite (backend/tests/) — run with: pytest
pytest==9.1.1
pytest-asyncio==1.4.0

# HTTP+WebSocket smoke testing against a real running dev server (not
# used by the app itself) — websockets also ships as a uvicorn[standard]
# extra, listed explicitly here since it's imported directly too.
httpx==0.28.1
websockets==17.0.1
